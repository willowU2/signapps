# Bug Sweep — Follow-up items

Items skipped during Phase A bug sweep (rollback double-failures, unreachable P2, etc.)
to be revisited in a later phase.

Format:
  ## <module>
  - <file:line> — <issue> — <reason skipped>

---

(No items yet — populated during execution.)
