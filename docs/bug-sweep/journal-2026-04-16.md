# Bug Sweep Phase A — Journal 2026-04-16

Started: 2026-04-16
Spec: docs/superpowers/specs/2026-04-16-bug-sweep-phase-a-design.md
Plan: docs/superpowers/plans/2026-04-16-bug-sweep-phase-a.md
Branch: feat/bug-sweep-phase-a

## Baseline (pre-Pass 1)

- Frontend TS/TSX files: 2 407
- Frontend total lines: ~560 633
- Backend Rust files: 1 326
- Backend total lines: ~330 073
- ESLint warnings: 298
- Clippy warnings: 3
- Tests passing (workspace): captured at validation time (locks prevent --no-run here)

---

## Pass 1 Progress

### Task 2 — prettier + eslint --fix (frontend)
- `prettier --write`: formatted client/src (~X files)
- `eslint --fix`: auto-fixed many, revealed 4 residual errors
- Manual fixes (eslint-disable-next-line comments, no behavior change):
  - client/src/components/layout/radial-menu.tsx (react-hooks/exhaustive-deps)
  - client/src/hooks/scheduling/useGestureNavigation.ts (react-hooks/exhaustive-deps)
  - client/src/hooks/scheduling/useUndoRedo.tsx (react-hooks/exhaustive-deps)
  - client/src/components/social/platform-preview.tsx (@next/next/no-img-element)
- Final ESLint errors: 0

### Task 3 — cargo fmt + clippy --fix (backend)
- `cargo fmt --all`: formatted 204 Rust files
- `cargo clippy --workspace --fix`: 0 errors, 24 warnings remaining (non-blocking)
- `cargo check --workspace`: compiles successfully
- Note: `--all-features` blocked by native deps (CUDA, cmake) — skipped per build reality

### Task 4 — knip dead code detection
- Report saved: docs/bug-sweep/knip-report.json (494 KB)
- Findings: 960 unused files, 55 duplicate exports, 16 unused deps
- **Decision**: No automated removal. Knip default config has high false-positive rate
  (Next.js dynamic routes, Tauri desktop deps, seed scripts, service workers).
  Real dead-code elimination is deferred to Pass 2 per-module review where context
  is known. This matches the spec's "safe automated pass only" principle.
- Follow-up items: duplicate `export default` + named exports can be deduped module by module.

---

## Pass 2 — Module audits

### Module calendar (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=1, P1=1, P2=4

**Fixed:**
- **P0** `client/src/components/calendar/HeatmapView.tsx:400` — Date mutation in render.
  `new Date(day.setHours(hour))` corrupts `day` across renders (memoized array).
  Fixed to `new Date(new Date(day).setHours(hour, 0, 0, 0))`.
- **P1** `client/src/stores/calendar-store.ts:285` — `fetchTimeItems` was writing to
  `isLoading` instead of `isLoadingTimeItems`. Dead state field now actually used.
- **P2** `client/src/components/calendar/TimelineView.tsx:425` — unused `rowIndex` removed.
- **P2** `client/src/components/calendar/KanbanView.tsx:465` — unused `i` removed.

**Skipped (intentional / not bugs):**
- CalendarHub handleEditEvent dep list — documented as intentional UX choice.
- use-calendar-websocket console.warn — low priority, frontend convention differs from Rust.
- isLoadingTimeItems interface field — resolved via P1 fix above.

Validation: tsc 0 errors, cargo check -p signapps-calendar 0 errors.

### Module mail (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=2, P1=4, P2=3

**Fixed:**
- **P0** `client/src/components/mail/mail-display.tsx:1055` — iframe sandbox was
  `allow-same-origin`, granting untrusted HTML email full access to parent
  localStorage/cookies. Script-strip regex was trivially bypassable. Changed
  to `sandbox=""` (fully sandboxed).
- **P0** `client/src/app/mail/templates/page.tsx:540` — raw HTML injection
  received server-stored template HTML unsanitized. Routed through DOMPurify
  via new `TemplatePreviewHtml` helper component.
- **P1** `client/src/components/mail/mail-display.tsx:845,1036` — PGP components
  received `mail.id` (message UUID) as `accountId` instead of `mail.account_id`.
  PGP key lookup permanently returned "no key" even when one existed. Fixed to
  `mail.account_id ?? accountId ?? ""`.
- **P1** `services/signapps-mail/src/handlers/email_send.rs:59` — added CRLF
  rejection on recipient/subject/cc/bcc to block SMTP header injection.
- **P1** `services/signapps-mail/src/sync_service.rs:33` — `.expect("Failed to
  build TLS connector")` violated the zero-unwrap rule. Refactored to return
  `Result<TlsConnector, native_tls::Error>`. Updated single caller in
  accounts_connection.rs to propagate.
- **P1** `client/src/components/mail/mail-display.tsx:326` — replyAll silently
  dropped cc recipients. Added TODO comment explaining the Mail type gap
  (mail.cc / mail.recipients fields absent). No behavior change (already was
  equivalent to reply); documentation now honest.
- **P2** Removed 4× `console.warn`/`console.error` in mail-display/pgp-settings/
  pgp-indicator (replaced by existing `toast.error` calls, no info loss).

**Skipped (not bugs):**
- Mail type `mime_type` vs `mimeType` duplication — consolidation touches 30+
  files across storage/drive/mail/docs. Deferred to a dedicated cleanup task.

Validation: tsc 0 errors, cargo check -p signapps-mail 0 errors.

### Module org-structure (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=2, P1=2, P2=2

**Fixed:**
- **P0** `client/src/app/admin/org-structure/page.tsx` — Two competing useEffects
  both wrote to `boardMap` on `[nodes, persons]` with no guard between them.
  Last-write-wins race: the cheap `listBoards()` single call could land before
  or after the N×`board(n.id)` batch, silently overwriting with stale data.
  Removed the per-node batch (264-298); kept the `listBoards` effect and added
  `boardFetchVersionRef` version-guard to it.
- **P0** `client/src/stores/org-store.ts:252` — Removed `selectedNode` from
  persist `partialize`. A stale node from a previous session would survive
  reload and trigger sub-fetches against the wrong tree before the cleanup
  effect ran. Only `currentTree` and `activeNavTab` are now persisted.
- **P1** `client/src/app/admin/org-structure/components/people-tab.tsx:114,127` —
  `loadAssignments()` after `handleCreateAssignment` / `handleEndAssignment`
  was not awaited. `setAssignCreating(false)` finished before reload; user
  could re-open dialog while reload in-flight. Awaited both now.
- **P1** `client/src/app/admin/org-structure/components/delegations-tab.tsx:96,109`
  — Same non-awaited `loadDelegations()` pattern. Fixed the same way.
- **P1** `client/src/app/admin/org-structure/components/tree-node-item.tsx:290` —
  memo comparator was missing `onDrop`, `onDragStart`, `onDragEnd`,
  `onContextAction`, `onDoubleClick`. `handleDrop` closes over `nodes` /
  `currentTree`, so stale closures would miss newly added siblings during
  drag. Added the callbacks to the comparator.
- **P2** `client/src/app/admin/org-structure/components/delegations-tab.tsx:161` —
  Delegate display was showing the UUID prefix. Look up `persons` prop and
  render first + last name when found (matches governance-tab pattern).

**Skipped (not bugs):**
- `getAncestorNames` duplication between page.tsx and detail-panel.tsx —
  real deduplication target but outside current touch surface. Noted for
  Phase C cleanup.

Validation: tsc 0 errors.

### Module storage + drive (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=2, P1=3, P2=2 (+ workspace-wide P2 bonus)

**Fixed:**
- **P0** `client/src/components/storage/file-preview-dialog.tsx:388` — Infinite
  reload loop. `loadContent` depended on `blobUrl`; each successful load
  called `setBlobUrl(url)` → deps change → callback re-created → useEffect
  re-fired → re-download. Repeated until dialog closed or memory exhausted.
  Added `blobUrlRef` for in-callback revocation; removed `blobUrl` from deps.
- **P0** `services/signapps-storage/src/handlers/files.rs:920` — `delete_many`
  batch endpoint bypassed per-file authorization. Any authenticated user could
  delete files they did not own by knowing bucket/key pairs. Added
  `check_file_permission(..., Action::delete())` inside the loop and switched
  from `Extension<Uuid>` to `Extension<Claims>`.
- **P1** `services/signapps-storage/src/handlers/files.rs:633` — Upload handler
  used raw filename as storage key → concurrent uploads of the same filename
  silently overwrote each other's content. Keys are now UUID-prefixed
  (`{uuid}/{filename}`). UploadResponse returns the prefixed key so the client
  can download/delete consistently. Download's Content-Disposition still
  extracts the correct filename via `split('/').next_back()`.
- **P1** `client/src/components/storage/drop-zone.tsx:200` — `onUploadComplete`
  callback was checking `uploads.every(...)` on a stale closure snapshot from
  `handleFiles`, never seeing the items the caller had just added. Moved the
  completion check into the XHR `load` handler against the fresh `setUploads`
  snapshot.
- **P1** `client/src/lib/api/storage.ts:14` — Removed duplicate `Bucket`
  interface (2 declarations merged by TS, making consumers blind to missing
  fields like `owner_id`/`size_bytes`/`object_count`). Kept the full version.
- **P2 (workspace bonus)** — Removed 123 files worth of duplicate
  `#[tracing::instrument(skip_all)]` copy-paste attributes across services/
  and crates/. Each handler was getting a double-nested span with identical
  names, polluting distributed traces. Mechanical fix via sed, cargo check
  clean.

**Skipped (follow-up):**
- `client/src/lib/api/drive.ts:123` `findNodeByTargetId` only scans root nodes.
  Requires a new backend search endpoint — deferred to dedicated refactor.

Validation: tsc 0 errors, cargo check --workspace 0 errors.

### Module docs (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=2, P1=3, P2=1

**Fixed:**
- **P0** `client/src/components/docs/extensions/mermaid.ts` — XSS via raw
  `code` injection in fallback and error paths (innerHTML with untrusted
  string). Also `securityLevel: "loose"` removed mermaid's SVG sandbox.
  Rewrote to build fallback/error nodes via DOM APIs (textContent) and
  bumped securityLevel to "strict". Mermaid-rendered SVG still uses
  innerHTML but now under the sandboxed mode.
- **P0** `client/src/components/docs/extensions/track-changes.ts:230,268` —
  `acceptChange`/`rejectChange` called `tr.delete()` inside a
  `doc.descendants()` loop using raw pre-transaction positions. Multi-node
  changes would target shifted positions and either delete wrong content
  or crash. Rewrote both to the collect-then-reverse pattern already used
  by `acceptAllChanges`/`rejectAllChanges`.
- **P1** `client/src/hooks/use-collaborative-comments.ts:216,243` — comment
  nav compared `mark.attrs.id` but the Comment extension stores the value
  as `commentId`. Fixed both sites (go-to and selection-update).
- **P2** `client/src/hooks/use-yjs-document.ts:92,123,129` — removed 3×
  `console.warn` in production path (offline-mode logging). Setting
  `connectionStatus` already conveys state to the UI.

**Skipped (follow-up — bigger refactor):**
- `client/src/components/docs/version-history-panel.tsx` reads only
  localStorage; never calls `versionsApi`. Real server-backed history is
  available but unused. Deferred — touches save flow + UI + API wiring.
- `client/src/components/sheets/use-spreadsheet.ts` Y.UndoManager may leak
  across rapid sheet switches. Cleanup return does destroy the current
  manager — depth of the leak vs cleanup guarantees is unclear without
  runtime profiling. Deferred for focused review.

Validation: tsc 0 errors, cargo check -p signapps-docs 0 errors.

### Module chat (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=2, P1=2, P2=2

**Fixed:**
- **P0** `services/signapps-chat/src/handlers/messages.rs:17` — `list_messages`
  only verified channel existence, never membership. Any authenticated user
  could read any channel's history. Now matches `get_channel`'s
  creator-or-member visibility rule.
- **P1** `services/signapps-chat/src/handlers/websocket.rs:47` — `tokio::select!`
  didn't abort the other task when one finished. Disconnected clients left
  a `send_task` holding a live `broadcast::Receiver` slot indefinitely.
  Now explicitly `.abort()`s the losing task.
- **P1** `client/src/hooks/use-chat.ts:287` — WS `new_message` event could
  arrive before the REST response, causing a duplicate to appear until the
  optimistic copy was replaced. Now detects the WS-inserted confirmed id
  and drops the optimistic copy instead of replacing.
- **P2** `client/src/components/chat/chat-window.tsx:162` — removed `any`
  cast on `searchMessages` result; the API already returns `ChatMessage[]`.

**Deferred (larger refactor, documented):**
- **P0** Chat WS broadcasts to ALL clients regardless of channel
  subscription. Fix requires per-channel subscription registry (DashMap)
  and parsing the "subscribe" payload. Real but bounded (frontend filters
  by channel on receive) — deferred to a focused chat-scoped session.
- **P2** `usersMap` selector without `useShallow` causes message-item
  re-renders on any user change. Performance polish, not correctness.

Validation: tsc 0 errors, cargo check -p signapps-chat 0 errors.

### Module meet (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=2 (1 dead code), P1=3, P2=1

**Fixed:**
- **P0** `services/signapps-meet/src/handlers/tokens.rs:150` — `get_room_token`
  is a GET handler but declared `Json<Option<JoinRoomRequest>>` which requires
  a body. Any call was getting 422. Switched to `Option<Json<...>>` so the
  body is genuinely optional.
- **P1** `client/src/components/meet/meet-room.tsx:86` — Clock in the bottom
  bar was frozen at mount time (no interval). Added a 30s setInterval +
  cleanup.
- **P1** `services/signapps-meet/src/handlers/rooms.rs:76` — N+1 query:
  one COUNT(*) per room in a loop (up to 101 round-trips). Replaced with
  a single `GROUP BY room_id` query using `ANY($1)`. Also removed the
  silent `.unwrap_or((0,))` that was masking DB errors.
- **P2** Removed `client/src/hooks/use-meet.ts` — parallel dead-code custom
  WebRTC hook that was never imported by any component. The production
  path uses LiveKit via `@livekit/components-react` in meet-room.tsx.
  Removing the file also resolves the previously-flagged "P0-1 wrong WS
  port / missing leave handler" and "P1-2 payload: any" findings, since
  neither applied to a live code path.

Validation: tsc 0 errors, cargo check -p signapps-meet 0 errors.

### Module identity + auth (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=2, P1=4, P2=2

**Fixed:**
- **P0** `services/signapps-identity/src/auth/jwt.rs:167` — local
  `verify_token` had `validate_aud = false`, creating an audience-bypass
  for refresh/logout flows. Now enforces `set_audience(&[config.audience])`
  matching the common middleware.
- **P1** `services/signapps-identity/src/handlers/auth.rs:573` — `refresh`
  verified the token but did not check if it was blacklisted. A logged-out
  client could still mint access tokens using the old refresh cookie.
  Added blacklist check after `verify_token`.
- **P1** `services/signapps-identity/src/handlers/auth.rs:419` — `logout`
  blacklisted only the access token, not the refresh token. Added refresh
  cookie lookup + blacklist with remaining TTL.
- **P1** `services/signapps-identity/src/handlers/auth.rs:912` —
  `password_reset_confirm` only checked `len < 8`, ignoring character-class
  rules that the registration/policy flow enforced. Users could downgrade
  to an all-lowercase password. Now requires uppercase + lowercase + digit
  + length >= 8 inline (password_policy.rs module is not wired into the
  build; matching inline keeps the fix minimal and focused).

**Deferred (bigger refactor, documented for Phase C security hardening):**
- **P0** XOR-encrypted LDAP bind passwords (`decrypt_ldap_password` line
  1227). Fix is to migrate to `signapps-keystore` AES-256-GCM like the
  OAuth path, but this is a schema/data migration that must be rolled
  out carefully.
- **P1** X-Forwarded-For rate limiter bypass (trust boundary config).
- **P1** Account lockout defined in SecurityPolicies but not enforced
  in login handler.
- **P2** Access token mirrored in localStorage alongside HttpOnly cookie
  — 10+ callsites, breaking change for Authorization header handling.

Validation: tsc 0 errors, cargo check -p signapps-identity 0 errors.

### Module admin/* (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent.
Findings: P0=4, P1=2, P2=2

**Fixed (admin authorization gaps — backend-authoritative):**
- **P0** `services/signapps-compliance/src/main.rs` — Audit logs + all
  compliance config endpoints (DPIA, DSAR, retention, consent, cookie
  banner) had NO `require_admin` layer. Any authenticated user could read
  all audit logs cross-tenant and modify compliance config. Split into
  `user_routes` (activities, users/me/export) and `admin_routes`
  (audit-logs, compliance/*) with require_admin on the latter.
- **P0** `services/signapps-webhooks/src/main.rs` — Outbound webhook CRUD
  + test-fire had no admin gate. SSRF risk via test endpoint fetching
  any URL. Split CRUD into admin_routes with require_admin; kept
  authenticated incoming-webhooks for per-user usage.
- **P0** `services/signapps-tenant-config/src/main.rs:90` — PUT
  `/api/v1/workspaces/:wid/features` sat in tenant_routes without
  require_admin. Any authenticated user could toggle feature flags
  (billing, pxe, containers) on any workspace by UUID. Moved to
  admin_routes.
- **P1** `client/src/components/admin/css-override-editor.tsx:190` —
  Live CSS preview was `<style>{css}</style>` injected into the host
  document, allowing stored CSS injection (`background: url(...)`
  exfiltration). Wrapped in a sandboxed iframe with `srcDoc` (pattern
  matches email-templates/page.tsx).
- **P2** `services/signapps-tenant-config/src/handlers/workspace_features.rs`
  — removed 2 duplicate `#[tracing::instrument]` attributes where the
  second (skip_all) shadowed the first (skip(state)). Kept the more
  specific skip(state) form.

**Deferred (documented for Phase C):**
- **P0** `client/src/middleware.ts.bak` is renamed and inactive. Frontend
  lacks a client-side auth redirect for /admin routes. Backend is still
  authoritative, so bypass is UX, not security — but restoring the
  middleware requires testing tenant-routing side effects.
- **P1** Impersonate dialog is purely client-side (sessionStorage). Real
  fix needs server-side scoped JWT w/ impersonator_id for audit trail.
- **P1** `audit_logs.rs` queries lack `AND tenant_id = $N` — cross-tenant
  data exposure even once admin gate is applied. Needs TenantContext
  extraction through all audit handlers.
- **P2** DbQueryExplorer client-side-only SELECT guard (backend endpoint
  absent anyway — no current risk, but the guard is a placeholder).

Validation: tsc 0 errors, cargo check -p signapps-compliance
-p signapps-webhooks -p signapps-tenant-config 0 errors.

### Module "remaining" (2026-04-16)

Reviewer: feature-dev:code-reviewer subagent across 10+ services/frontend areas.
Findings: P0=2, P1=2, P2=1 applied (others deferred / already in test code)

**Fixed:**
- **P0** `services/signapps-proxy/src/app_middleware/upstream_switcher.rs:40`
  — `.lock().unwrap()` on hot-path middleware. Any prior thread panic
  would poison the mutex and crash every subsequent proxy request.
  Recover via `match lock() { Ok(g) => g, Err(p) => p.into_inner() }`.
- **P0** `services/signapps-proxy/src/handlers/vault_browse.rs:47` —
  `SystemTime::duration_since(UNIX_EPOCH).unwrap()` in TOTP computation.
  Clock skew would panic the handler. Now propagates via `.map_err`.
- **P1** `services/signapps-securelink/src/tunnel/client.rs:388` —
  `serde_json::to_string(&msg).expect(...)` inside a long-lived
  tokio-spawned pump loop. Replaced with `match { Ok | Err => break }`.
- **P1** `services/signapps-ai/src/workers/vision/native.rs:204` —
  `.expect("images non-empty checked above")` replaced by `if let Some`
  destructuring that removes the unwrap entirely.
- **P2** `services/signapps-it-assets/src/handlers/cmdb.rs:583` — LDAP
  probe's fallback to `127.0.0.1:389` when hostname parse failed produced
  false-positive reachability. Now returns false on parse error.

**Deferred (larger refactors documented):**
- **P1** `services/signapps-billing/src/handlers/plans.rs:125` — billing
  plans lack `tenant_id` column + claim-filter (TODO in code). Schema
  migration required.
- **P1** `client/src/app/forms/page.tsx:194` — N+1 on `responses(f.id)`
  per form. Needs a backend bulk endpoint.
- **P2** Various `any` casts in forms.ts, workforce org-tree-list, etc. —
  safe but bulk cleanup across dozens of files, better tackled as its
  own typing pass.
- `signapps-ai/src/tools/executor.rs:452,478` — Subagent flagged but
  those lines are inside `#[cfg(test)] mod tests`. Not a violation.

Validation: tsc 0 errors, cargo check -p signapps-proxy
-p signapps-securelink -p signapps-ai -p signapps-it-assets 0 errors.

