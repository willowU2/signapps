//! S4U2Self and S4U2Proxy constrained delegation.

/// S4U2Self: service obtains a ticket on behalf of a user.
///
/// Will be implemented when constrained delegation is needed.
///
/// # Panics
///
/// No panics — all errors are propagated via `Result`.
pub async fn handle_s4u2self() {
    // Will be implemented when constrained delegation is needed
}

/// S4U2Proxy: service accesses another service on behalf of a user.
///
/// Will be implemented when constrained delegation is needed.
///
/// # Panics
///
/// No panics — all errors are propagated via `Result`.
pub async fn handle_s4u2proxy() {
    // Will be implemented when constrained delegation is needed
}
