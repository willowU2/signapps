//! Provider definitions (embedded + DB override).

/// Full provider definition as embedded in the catalog JSON.
///
/// Placeholder — fully defined in Task 4.
#[allow(dead_code)]
pub struct ProviderDefinition;

/// Runtime-merged provider config: catalog defaults + tenant DB overrides.
///
/// Placeholder — fully defined in Task 4.
#[allow(dead_code)]
pub struct ProviderConfig;

/// Lightweight summary for listing providers in UI/API responses.
///
/// Placeholder — fully defined in Task 4.
#[allow(dead_code)]
pub struct ProviderSummary;
