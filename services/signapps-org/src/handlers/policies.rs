//! CRUD handlers for `/api/v1/org/policies` — canonical surface (S1 W2).
//!
//! Wired in Task 11. Placeholder `routes()` returns an empty router.

use axum::Router;

use crate::AppState;

/// Routes for `/api/v1/org/policies` — placeholder in the scaffolding task.
pub fn routes() -> Router<AppState> {
    Router::new()
}
