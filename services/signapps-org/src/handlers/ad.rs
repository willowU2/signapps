//! AD / LDAP configuration & sync handlers for `/api/v1/org/ad` (S1 W3).
//!
//! Wired in W3. Placeholder `routes()` returns an empty router.

use axum::Router;

use crate::AppState;

/// Routes for `/api/v1/org/ad` — placeholder in the scaffolding task.
pub fn routes() -> Router<AppState> {
    Router::new()
}
