//! CRUD handlers for `/api/v1/org/boards` — canonical surface (S1 W2).
//!
//! Wired in Task 11. Placeholder `routes()` returns an empty router.

use axum::Router;

use crate::AppState;

/// Routes for `/api/v1/org/boards` — placeholder in the scaffolding task.
pub fn routes() -> Router<AppState> {
    Router::new()
}
