//! IMAP server listener for the mail service.
//!
//! Provides an IMAP server stub on port 993 (IMAPS). The full IMAP command
//! parser and session handler will be implemented in a later sprint.

pub mod server;
