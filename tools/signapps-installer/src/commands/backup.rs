//! `backup` — fleshed out in Task P4.8.

use std::path::PathBuf;

pub async fn run(
    _config_dir: Option<PathBuf>,
    _output_dir: Option<PathBuf>,
) -> anyhow::Result<()> {
    anyhow::bail!("not yet implemented")
}
